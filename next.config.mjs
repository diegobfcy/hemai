/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/mi-proyecto',
  assetPrefix: '/mi-proyecto/',
  images: {
    unoptimized: true
  }
};

export default nextConfig;
